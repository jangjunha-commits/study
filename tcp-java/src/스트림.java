public class 스트림 {
    public static void main(String[] args) {
        /*
        스트립 파일이나 콘솔 입출력을 다루지 않고 스르미이라는 흐름을 통해 다룹니다
        스르팀 -> 실제의 입력이나 출력이 표현도니 데이터의 이상화된 흐름을 의미합니다
        입출력 스트립
        스트림 -> 한방향으로 통신 할수 있으므로 입력과 출력을 동시에 처리할수 없다
        IuputStream클래스 read()메소드가 OutputStream크랠스 write()메소드
        각각 추상 메소드 포함되어 있다
        InputStram
        abstract int read() 해당입력스틤으로부터 다음 바이트를 읽어들임
        int read(byte[] b)
        int read(byte[] b, int off, int len)
        해당입력스트림으로부터 특정바이트 앍어들인후 배열 b에 저장함
        해당입력스틈임으로부터 len바이트를 일겅들인후 배열 b[off]부터 저장함
        OutPutStream abstract 해당 출력 스트림에 특정 바이트를 저장함
        void write(byte[] b) 배열b의 특정바이트를 배열b의 길이만큼 해당 출력 스트림에서 저장함
        void write(byte[] b, int off, int len) 배열b[off]부터 len바이트를 해당 출력 스트립에 저장함
         */
        /*

         */
    }
}
