package bronze5;

import java.util.Scanner;

public class 문자열 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int T = sc.nextInt(); //입력할 숫자 개수 3
        for (int i=0; i<T; i++) {
            String text = sc.nextLine(); //한줄씩 출력하기
            /*String answer =
                    text.substring(0,1) * //첫째 문자열 이름
                    text.substring(text.length()-1); //마지막 문자열 이름
            System.out.println(answer);*/
        }
    }
}
