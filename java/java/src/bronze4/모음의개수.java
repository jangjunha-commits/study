package bronze4;

import java.util.Scanner;
/*
public class 모음의개수 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        while(true) { //계속 입력
            String text = sc.nextLine();
            if(text.equals("#")) { //#이면 종료
                break;
                */
            /*
            int count = 0; //개수(모음)
            char[] textArr = text.toCharArray(); //한줄 한문자 입력
            for(int i=0; i<textArr.length; i++) { //모음의 조건
                if(textArr[i]=="a" || textArr[i]=="e"|| textArr[i]=="i" || textArr[i]="o" || textArr[i]="u"||
                        textArr[i]=="A" || textArr[i]=="E"|| textArr[i]=="I" || textArr[i]="O" || textArr[i]="U") {
                    count++; //개수 세기
                }
            }
            System.out.println(count); //모음의 개수 출력
        }
    }
}
*/
